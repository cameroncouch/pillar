- Remove inline styling
- Stop shelf from scaling when clicking on child elements
- Set up drag styling to be a calculation based on box-model values
- Don't allow drag off-screen
- Make it WCAG compliant
- Set up animations
- Set up demo callbacks on the shelf buttons
- structure config.json by property types (style, behavior, markup)